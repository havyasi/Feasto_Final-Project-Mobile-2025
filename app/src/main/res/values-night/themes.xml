<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FooDrink" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="android:windowBackground">@drawable/dark_bg4</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Custom CardView style for night mode -->
    <style name="Widget.Foodrink.CardView" parent="Widget.MaterialComponents.CardView">
        <item name="cardElevation">4dp</item>
        <item name="cardCornerRadius">8dp</item>
        <item name="strokeWidth">1dp</item>
        <item name="strokeColor">@color/card_stroke</item>
    </style>

    <!-- Glassmorphic card style for night mode -->
    <style name="Widget.Foodrink.GlassmorphicCard" parent="Widget.MaterialComponents.CardView">
        <item name="cardElevation">0dp</item>
        <item name="cardCornerRadius">24dp</item>
        <item name="cardBackgroundColor">#33FFFFFF</item>
        <item name="strokeWidth">1dp</item>
        <item name="strokeColor">#66FFFFFF</item>
    </style>
</resources>